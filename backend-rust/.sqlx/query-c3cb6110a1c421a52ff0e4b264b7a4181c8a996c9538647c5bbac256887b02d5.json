{
  "db_name": "PostgreSQL",
  "query": "\n                    INSERT INTO account_tokens (index, index_per_token, account_index, token_index, balance)\n                    VALUES (\n                        (SELECT COALESCE(MAX(index) + 1, 0) FROM account_tokens),\n                        (SELECT COALESCE(MAX(index_per_token) + 1, 0) FROM account_tokens WHERE token_index = (SELECT index FROM tokens WHERE token_address = $2)),\n                        (SELECT index FROM accounts WHERE address = $1),\n                        (SELECT index FROM tokens WHERE token_address = $2),  \n                        $3\n                    )\n                    ON CONFLICT (token_index, account_index)\n                    DO UPDATE SET balance = account_tokens.balance + EXCLUDED.balance",
  "describe": {
    "columns": [],
    "parameters": {
      "Left": [
        "Bpchar",
        "Text",
        "Numeric"
      ]
    },
    "nullable": []
  },
  "hash": "c3cb6110a1c421a52ff0e4b264b7a4181c8a996c9538647c5bbac256887b02d5"
}
