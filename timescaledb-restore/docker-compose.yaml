version: "3"
services:
  timescaledb:
    image: timescale-restore:1
    build: .
    environment:
    - POSTGRES_PASSWORD=password
    - PGBACKREST_CONFIG=/etc/pgbackrest/pgbackrest.conf
    ports:
    - 15432:5432    
    networks:
    - ccdscan
    volumes:
    - ./init/pgbackrest.conf:/etc/pgbackrest/pgbackrest.conf
networks:
  ccdscan:
